#include "PlayerAgent.h"

namespace Tools {
	namespace Agents {
		void PlayerAgent::ReceivedUpdatePhysics(const CNetwork::PhysicsUpdateData &data) {
			/* Nothing, player agent doesn't listen to packets */
		}
	}
}